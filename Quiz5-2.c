
#include <stdio.h>
#include <math.h>
# define PI 3.1415926

int main() {
    
    /*공식을 모르겠음 ㅋㅋㅋㅋ */
    // double roadWidth = 1.0; // 도로의 폭 (1m)
    // double wheelDistance = 0.7; // 두 바퀴 사이의 거리 (0.7m)
    // double desiredAngle = 90.0; // 원하는 회전 각도 (90도)
    // double radius; // 바퀴 지름
    // double wheelThickness = 0.15; // 바퀴 두께

    // printf("바퀴 지름 입력해라 (미터): ");
    // scanf("%lf", &radius);

    // // 원의 반지름 계산
    // double circleRadius = (radius - wheelThickness) / 2.0;

    // // 호의 길이 계산
    // double arcLength = wheelDistance / 2.0;

    // // 호의 길이를 반지름으로 나눈 값 (라디안)
    // double radianAngle = arcLength / circleRadius;

    // // 라디안을 도로 각도로 변환
    // double degreeAngle = radianAngle * (180.0 / PI);

    // // 회전 각도를 바퀴 한 바퀴 돌 때 회전수로 나눈 값
    // double rotationCount = degreeAngle / 360.0;

    // printf("바퀴 지름: %.2lf 미터\n", radius);
    // printf("바퀴 두께: %.2lf 미터\n", wheelThickness);
    // printf("좌측 바퀴의 회전 각도: %.2lf 도\n", degreeAngle);
    // printf("우측 바퀴의 회전 각도: %.2lf 도\n", degreeAngle);
    // printf("좌측 바퀴의 회전수: %.2lf 바퀴\n", rotationCount);
    // printf("우측 바퀴의 회전수: %.2lf 바퀴\n", rotationCount);

    return 0;
}
